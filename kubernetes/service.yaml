apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  labels:
    app.kubernetes.io/name: portfolio
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: portfolio
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http


